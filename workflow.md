시열님, 설계도와 아키텍처가 집의 뼈대라면 **워크플로우(Workflow)**는 그 집 안에서 사람들이 어떻게 움직이는지 보여주는 '동선'입니다.

현장 관리자가 앱을 켜서 퇴근할 때까지, 그리고 월말에 정산할 때까지의 흐름을 **사용자 경험(UX) 중심**으로 정리한 `workflow.md`입니다. Antigravity(AI)가 이 흐름을 이해하면 버튼 하나하나의 배치 이유를 정확히 알게 될 거예요.

---

# 🔄 System Workflow: From Field to Settlement

이 문서는 사용자가 앱을 조작하는 순서와 그에 따른 시스템의 내부 동작 흐름을 정의한다.

## 1. 초기 설정 단계 (Initial Setup)
사용자가 앱을 처음 실행하거나 새로운 프로젝트를 시작할 때의 흐름이다.

1.  **현장 등록(Site Creation):** 관리자가 현재 관리할 현장명(예: 고덕 A블록)을 입력한다.
2.  **인력 풀 구성(Worker Onboarding):**
    * [일용직] 또는 [상용직] 타입을 선택한다.
    * 이름, 생년월일, 단가(일당/월급)를 입력한다.
    * 상용직의 경우 식대, 자가운전 등 **비과세 항목**을 초기 세팅한다.
3.  **데이터 저장:** 입력된 정보는 `localStorage`의 `workers`와 `sites` 테이블에 영구 저장된다.

---

## 2. 일일 현장 관리 단계 (Daily Field Operation)
가장 빈번하게 일어나는 핵심 워크플로우다.

1.  **날짜 선택:** 메인 달력에서 '오늘' 또는 '기록할 날짜'를 터치한다.
2.  **출근 체크 (The 'Tap' Action):**
    * 화면 하단에서 올라온 인력 리스트에서 오늘 출근한 사람의 이름을 터치한다.
    * **System Check:** 해당 근로자의 이번 달 누적 일수를 실시간 계산한다.
    * **Risk Alert:** 만약 이번 클릭으로 8일째가 된다면, 이름 옆에 **⚠️ 8일 도달** 아이콘을 즉시 표시한다.
3.  **실시간 합계:** 화면 하단 대시보드에 '오늘의 총 노무비'와 '투입 인원수'가 즉시 업데이트된다.

---

## 3. 리스크 모니터링 단계 (Risk Monitoring)
관리자가 의사결정을 내리는 시점의 흐름이다.

1.  **누적 데이터 분석:** 관리자가 특정 인원을 클릭하여 상세 정보를 본다.
2.  **공제 시뮬레이션:** * 시스템은 `logic_manifesto`에 따라 국민연금, 건강보험 가입 여부를 판단한다.
    * 보수 총액이 220만 원을 넘었는지 체크하여 **[보험료 발생 예고]** 메시지를 보여준다.
3.  **인력 조정:** 관리자는 알림을 보고 해당 인원을 다른 현장으로 배치하거나 출근 일수를 조절한다.

---

## 4. 월간 정산 및 보고 단계 (Monthly Settlement & Export)
한 달 공사가 끝나고 비용을 지급하는 시점의 흐름이다.

1.  **리포트 생성:** '정산' 탭으로 이동하여 해당 월을 선택한다.
2.  **정산 내역 확인:**
    * 개인별: [총액 - 공제액(4대보험/세금) = 실수령액]을 확인한다.
    * 현장별: 전체 투입 비용 합계를 확인한다.
3.  **공유 및 저장:**
    * **이미지 저장:** 명세서 영역을 캡처하여 갤러리에 저장한다.
    * **카톡 전송:** 저장된 이미지를 근로자 또는 본사 경리팀에 카카오톡으로 전송한다.

---

## 5. 시스템 내부 로직 흐름 (Internal Logic Flow)



1.  **Event Trigger:** 사용자의 클릭(Click) 또는 입력(Input) 발생.
2.  **Controller:** `Data.js`가 이벤트를 수신하고 현재 상태(State)를 업데이트.
3.  **Logic Engine:** `Calc.js`가 업데이트된 데이터를 바탕으로 보험료/세금을 재계산.
4.  **UI Renderer:** `View.js`가 계산된 결과값을 받아 화면의 특정 숫자와 아이콘을 갱신.
5.  **Auto-Save:** 모든 변경사항은 0.5초 이내에 `localStorage`에 JSON으로 직렬화되어 저장.

---

### 💡 시열님(CEO)을 위한 워크플로우 활용 가이드

시열님, 이 워크플로우를 Antigravity에 전달할 때 이렇게 말씀해 보세요.

> "**방금 준 `workflow.md`를 읽어줘.**
> 
> 1. 특히 **2번(일일 관리)**과 **4번(정산 및 보고)** 흐름이 끊기지 않게 UI를 설계해줘.
> 2. 출근 체크를 할 때마다 백그라운드에서 **3번(리스크 모니터링)** 로직이 돌아서 관리자에게 경고를 주는 게 핵심이야.
> 3. 사용자가 실수로 앱을 꺼도 데이터가 날아가지 않게 **5번(Auto-Save)** 기능을 확실히 구현해줘."

---

