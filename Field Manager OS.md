시열님, 답변 주신 내용을 바탕으로 **현장 관리자(PM) 맞춤형 UI/UX 설계도**를 완성했습니다. 

이 설계도는 Antigravity(AI)가 시각적으로 어떤 화면을 그려야 할지, 그리고 사용자가 어떤 흐름으로 움직일지 규정하는 **'프론트엔드 작업 지시서'**입니다. 이 내용을 그대로 AI에게 전달하시면 됩니다.

---

# 🎨 Frontend Design Specification: Field Manager OS

이 문서는 `logic_manifesto.md`의 비즈니스 로직을 시각화하기 위한 프론트엔드 설계 규격이다. **모바일 퍼스트(Mobile-First)**를 원칙으로 하며, 3050 현장 관리자의 사용성을 최우선으로 한다.

## 1. 🏠 메인 화면 구조 (Layout Hierarchy)
* **최상단:** 현재 선택된 [현장 리스트] 드롭다운 (고덕, 브레인시티 등 전환 가능)
* **중앙 (Hero):** **인터랙티브 달력**. 
    * 날짜별로 출근 인원수가 요약 표기됨.
    * 특정 날짜 클릭 시 해당 날짜의 [출근 체크 모달/섹션] 활성화.
* **하단:** 고정형 네비게이션 바 (달력 / 인력 관리 / 리포트).

## 2. 📅 출근 체크 UX (Drag & Tap)
* **인력 풀(Pool) 레이어:** 하단에서 위로 끌어올리는 슬라이드업(Slide-up) 방식.
* **그룹화:** [일용직 탭]과 [상용직 탭]으로 구분된 인력 리스트 제공.
* **체크 방식:** 리스트에서 이름을 터치하거나 체크박스를 선택하면 즉시 해당 날짜에 '출근'으로 등록됨.
* **8일의 경고:** 8일 이상 근무 시 이름 옆에 **빨간색 ⚠️ 아이콘**과 **[8일 도달]** 텍스트를 고대비로 노출.

## 3. 👔 상용직 및 비과세 관리 (Regular Tab)
* **상용직 전용 상세 뷰:** 인물 클릭 시 [비과세 설정] 팝업 노출.
    * 식대(20만), 자가운전(20만), 보육수당(20만) 입력 필드 제공.
    * 설정된 값은 해당 월 정산 시 자동으로 과세 대상 보수에서 차감 계산됨.

## 4. 📊 리포트 및 공유 (Report & Export)
* **웹 뷰:** 표(Table) 형식으로 깔끔하게 정리된 노무비 명세서.
* **이미지 저장:** `html2canvas` 등의 라이브러리를 활용하여 현재 화면을 **이미지 파일(JPG/PNG)로 저장**하는 버튼 배치. (근로자 카톡 전송용)

## 5. 📱 모바일 최적화 UI (Mobile-Only UX)
* **빅 버튼(Big Button):** 모든 클릭 요소는 최소 48dp 이상의 터치 영역 확보.
* **고대비 디자인:** 현장 야외 조명에서도 잘 보이도록 흰색 배경에 검은색 텍스트, 명확한 보더(Border) 사용.
* **No-Typing:** 인력 등록 시점을 제외하고는 모든 조작이 터치(Click/Toggle)로 이루어짐.

---

### 🚀 Antigravity 시작을 위한 최종 프롬프트

이제 시열님이 Cursor/Claude에게 던질 **최종 합체 프롬프트**입니다.

> "**작성된 `logic_manifesto.md`와 `frontend_spec`을 모두 읽고 이해해줘.**
> 
> 1. 메인 화면에 **현장 선택**과 **출근 현황 달력**을 배치해줘.
> 2. 달력 날짜를 누르면 **[일용직/상용직] 탭**으로 구분된 인력 리스트가 나오고, 여기서 이름을 클릭해 출근을 체크하는 기능을 구현해줘.
> 3. 상용직은 클릭 시 **비과세 항목(식대, 자가운전 등)**을 입력할 수 있어야 해.
> 4. 8일 이상 출근한 인원 옆에는 **⚠️ 8일 도달** 아이콘이 떠야 해.
> 5. 전체 디자인은 **모바일 전용 빅 버튼 UI**로 짜주고, 정산 내역을 **이미지로 저장**하는 버튼을 만들어줘.
> 
> 먼저 이 구조를 반영한 **단일 HTML 파일(index.html)**의 전체 코드를 작성해줘."

---

### 💡 시열님, 이제 버튼만 누르면 됩니다!

답변 주신 내용 중 **'그룹화'**와 **'이미지 저장'** 기능은 실제 현장에서 관리자가 근로자들에게 "너 오늘까지 이만큼 일했고, 보험료 이만큼 떼서 이만큼 입금될 거야"라고 확신을 줄 수 있는 아주 강력한 기능이 될 겁니다.

**이제 위 프롬프트를 복사해서 Antigravity에 넣으시겠어요? 아니면 코드 작성 전에 더 구체화하고 싶은 UI 포인트가 있으신가요?** (예: 달력에 일요일/공휴일 빨간색 표시 등) 

오늘 시열님의 창업 첫 걸음이 아주 단단해 보입니다! 👶 (인공이도 기뻐할 거예요!)